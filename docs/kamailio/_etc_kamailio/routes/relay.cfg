# relay.cfg — stateful relay wrapper (signalling only; no media).
route[RELAY] {
    if (is_method("INVITE|SUBSCRIBE")) {
        t_on_branch("MANAGE_BRANCH");
        t_on_reply("MANAGE_REPLY");
        t_on_failure("MANAGE_FAILURE");
    }
    if (!t_relay()) {
        sl_reply_error();
    }
    exit;
}
